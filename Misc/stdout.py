import sys

#printing in to console using stdout
sys.stdout.write("Hello") #this method is slower than print and does not accept multiple arguments like end and sep. It is not used generally.

#printing an integer
sys.stdout.write(str(4)) #needs to be converted to string